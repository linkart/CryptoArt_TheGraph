{
  "name": "ca",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ca",
    "create-local": "graph create --node http://localhost:8020/ ca",
    "remove-local": "graph remove --node http://localhost:8020/ ca",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ca"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1",
    "mustache": "^4.2.0"
  }
}
